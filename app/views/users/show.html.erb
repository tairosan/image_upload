4<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Photo:</b>
  <%= image_tag(@user.photo_url) if @user.photo? %>
  <%= image_tag(@user.photo.thumb.url) if @user.photo? %>
</p>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
